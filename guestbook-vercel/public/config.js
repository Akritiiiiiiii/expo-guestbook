/* Local settings. Paste your deployed contract address below,
   or leave this empty and set CONTRACT_ADDRESS in Vercel instead.

   Other options you can override here:
     chainId, chainName, rpcUrl, explorer, faucetNote, pageSize, pollMs
*/
window.GUESTBOOK_CONFIG = {
  contractAddress: ""
};
